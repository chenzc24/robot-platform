# MaixCam and ESP32 drawing payload deployment - 2026-09-09

Source payload: `drawing-20260908-d2469eb`.
Deployment-record commit: pending at record creation.

## Outcome

The MaixCam arm/video and ESP32 runtime payloads were backed up, published and
hash-verified. The robot-arm controller project was not imported or started by
this deployment; the operator reported its service suspended. No device motion
command was sent.

## MaixCam

- Identity: `maixcam-6c7d`, riscv64, Buildroot, Linux 5.10.4.
- Pre-deployment state: only the launcher-owned `num` application was present;
  TCP 8780 and RTSP 8554 were not listening.
- Backup: ignored local
  `device-backups/maixcam/20260909-pre-d2469eb`, containing 32 arm files and 12
  video files. The real `arm_service_config.py` was preserved byte-for-byte.
- Staged release: `/root/robot-platform/releases/20260909-d2469eb`.
- All eight arm and seven video payload hashes matched the package manifest in
  staging and after activation. Twelve target Python files compiled and four
  shell files passed syntax checks.
- Final observation: guarded wrapper PID 967, arm gateway PID 979 as sole UART0
  owner/listener on 8780, and RTSP PID 983 on 8554. These PIDs are evidence only,
  not reusable process targets. PC status reported the camera online.
- No PING, STATUS or motion request was sent through the gateway to the suspended
  robot-arm controller.

## ESP32

- Recovery/identity: CH340 on COM7; ESP32_GENERIC_S3-SPIRAM_OCT, MicroPython
  1.27. USB entry reached a safe REPL prompt and restored WebREPL.
- Backup: all 43 device files copied to ignored local
  `device-backups/esp32/20260909-pre-d2469eb` before upload.
- Published all 14 manifest files to the flat root. All compiled on-device and
  all post-upload readback hashes matched. `device_config.py` and `secrets.py`
  matched their backup bytes and were not replaced.
- One reset completed. Authenticated RCP/TCP v3 WELCOME/STATUS reported
  `service_state=ready`, `chassis_state=disabled`, `motion_permitted=true`,
  `hold_remaining_ms=0`, and `last_error=none`.
- The preserved configuration is `RUNTIME_MODE=tcp_v3_l3` and
  `L3_MOTION_PERMITTED=True`. Current `main.py` starts that runtime based on
  `RUNTIME_MODE` after emitting the application safe-idle status, independently
  of `RUN_MODE=safe_idle`. This is recorded explicitly so the status prelude is
  not mistaken for a motion-disabled L2 composition.
- No ENABLE, VELOCITY, STOP, DISABLE or line-follow request was sent.

## Recovery and remaining gates

- MaixCam recovery restores the dated arm/video backup and prior launcher
  ownership. ESP32 recovery restores the dated full-root backup through COM7;
  no firmware erase was performed.
- The full package remains `deployed=false` because its DobotStudio controller
  project was not imported in this operation.
- Baseline offset, direction/distance validation and any arm/chassis movement
  remain L3/L4 work requiring a fresh attended safety gate and physical emergency
  stop. The first coordinated run must not be the complete 439-stroke job.
