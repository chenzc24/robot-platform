# Baseline deployment candidate and branch cleanup

- Status: `completed`
- Responsible: `agent`
- Highest validation level: `L1`

## Objective

Freeze the latest merged `main` source needed by MaixCam, ESP32 and the robot
arm into a hash-addressed deployment candidate, build the DobotStudio YOLO
project locally, document the exact baseline test boundary, and remove only
fully merged obsolete drawing work branches/worktrees.

## Initial workspace state

- PR #10 merged cleanly into remote `main` as `301320c`.
- This isolated worktree started clean from that commit on
  `target/baseline-deployment-readiness`.
- The primary `main` worktree is behind remote and contains unrelated user and
  collaborator changes, including a local `app/demo.py` speed edit. It remains
  untouched.
- The drawing planner, line-following core and drawing-control-mode branches are
  ancestors of `origin/main` and their worktrees were clean.
- Draft PR #2 depends on `target/chassis-hold-release-fix` and
  `target/arm-fault-foundation`; both branches and the latter worktree remain.

## Modifiable scope

- `docs/deployment/README.md`
- new dated candidate manifest and readiness document under `docs/deployment/`
- this plan and an append-only `plan/log.md` entry
- ignored `build/` output generated by the existing robot-arm builder
- merged local/remote drawing branches and their clean isolated worktrees

## Read-only scope

- all runtime/protocol/application source, device-local configuration and
  device files
- primary worktree dirty paths and raw-resource archives
- device addresses, credentials, TCP232 configuration, taught points and
  controller safety configuration

## Candidate boundary

- ESP32 baseline uses direct RCP/TCP v3 velocity, hold expiry, STOP and health
  timeout. The optional line follower remains disabled for this run.
- MaixCam provides the arm command gateway and video service; AprilTag is not a
  baseline dependency.
- The DobotStudio output is generated with `--yolo`, because the current PC arm
  primitive route requires the reviewed engineering mode.
- The existing `app/demo.py` can run a bounded single-canvas arm drawing. The
  newly merged relocation strategy is a tested planner boundary, not yet a full
  439-stroke arm/chassis executor or UI route. Do not claim end-to-end baseline
  readiness beyond the selected bounded test.

## Validation and safety

- No device connection, upload, reset, service stop/start, controller import or
  motion in this goal.
- Verify every tracked candidate file and SHA-256, build the ignored arm
  package, compare it to a second deterministic build, compile/parse sources,
  run relevant L1 tests, and audit Git scope.
- Actual deployment starts with backups and non-motion L2 checks. Any following
  chassis or arm movement needs a new current-test L3/L4 physical safety gate.

## Actual results

- PR #10 merged into remote `main` as `301320c`. The three merged drawing
  branches and two clean obsolete worktrees were removed locally and remotely.
  Draft PR #2's head/base branches and worktree were retained.
- Added a hash-addressed candidate manifest covering 14 ESP32, 15 MaixCam and
  7 robot-arm source/build inputs. Every source hash was recomputed from
  `301320c` and matched the manifest.
- Generated `build/baseline-301320c-arm-yolo` twice. All four DobotStudio
  outputs were byte-identical; Python compilation and JSON parsing passed.
- L1 passed 209 relevant tests: protocol 28, ESP32 76, MaixCam 57, robot arm 18,
  and drawing app/control 30. The 40-file Python source check and four MaixCam
  shell syntax checks passed. Candidate/generated JSON and `git diff --check`
  passed.
- No device connection, upload, controller import, reset, service operation,
  configuration write or motion occurred.
- The candidate documents a remaining functional boundary: `app/demo.py` is a
  runnable single-canvas arm path, while the Baseline chassis relocator is an
  L1-tested library/checkpoint adapter. A guarded full grouped arm/chassis
  executor is still required before a coordinated L4 Baseline run.

## Intent to submit

```text
docs(deploy): freeze baseline drawing deployment candidate
```
