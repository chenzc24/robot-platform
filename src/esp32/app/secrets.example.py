"""Copy to secrets.py and fill locally; secrets.py is ignored by Git."""

WIFI_SSID = "replace-with-local-hotspot-name"
WIFI_PASSWORD = "replace-with-local-hotspot-password"
WEBREPL_PASSWORD = "4-to-9-characters"

# A separate local RCP/TCP v2 credential. Keep it only in ignored secrets.py.
RUNTIME_CREDENTIAL = "replace-with-local-runtime-credential"
